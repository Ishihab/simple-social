# msgpack (GHSA-6v7p-g79w-8964) and setuptools (CVE-2025-47273):
# vendored inside pip's own bundled dependencies in the official
# python:3.13-slim-trixie base image itself — confirmed by scanning
# the bare upstream image directly (not this project build). Not installed
# via uv/pip in our app, not present in .venv, not fixable from
# project Dockerfile. Tracking upstream: fixed once the Python Docker
# team updates their bundled pip.
GHSA-6v7p-g79w-8964
CVE-2025-47273
CVE-2026-53615